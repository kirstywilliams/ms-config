# Config files for a project toying around with the Microservice-style Architecture

## Convention

```
<application>-<profile>.yml
<application>-<profile>.properties
```

E.g. foo-development.yml or foo-development.properties